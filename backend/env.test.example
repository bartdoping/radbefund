# Test Environment Configuration
NODE_ENV=test
PORT=3002
USE_HTTPS=false

# Database (Test)
DATABASE_URL=postgresql://test_user:test_password@localhost:5433/radbefund_test
DB_HOST=localhost
DB_PORT=5433
DB_NAME=radbefund_test
DB_USER=test_user
DB_PASSWORD=test_password
DB_SSL=false

# Redis (Test)
REDIS_URL=redis://localhost:6380
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=test_redis_password
REDIS_DB=1

# JWT (Test)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h
BCRYPT_ROUNDS=4

# AI (Mock for tests)
PROVIDER=openai
OPENAI_API_KEY=test-key
OPENAI_MODEL=gpt-4o-mini

# Rate Limiting (Relaxed for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000
AUTH_RATE_LIMIT_WINDOW_MS=60000
AUTH_RATE_LIMIT_MAX_REQUESTS=100

# Monitoring
PROMETHEUS_PORT=9091
LOG_LEVEL=error

# Cache TTL (Short for tests)
CACHE_TTL_USER_SESSION=300
CACHE_TTL_LAYOUT_TEMPLATE=300
CACHE_TTL_AI_RESPONSE=60
CACHE_TTL_USER_PREFERENCES=300

# Security
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
HELMET_ENABLED=false
